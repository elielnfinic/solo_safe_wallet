/// This file contains the logic for handling transactions.
/// This includes creating transactions, getting transactions, and updating transactions.
/// This file also contains the logic for handling the transaction history.
library;


Future<bool> mintAsset({required String amount, required String to}) async {
  // final walletKit = WalletKit();
  // final result = await walletKit.mintAsset(amount: amount, to: to);
  // return result;
  return false;
}